{
  "service": {
    "name": "Health Check: CLAMP",
    "check":[
      {
        "id"  : "clamp-health",
        "name": "Clamp Health Check",
        "http": "http://clamp.onap-clamp:8080/restservices/clds/v1/clds/healthcheck",
        "tls_skip_verify": false,
        "method": "GET",
        "interval": "10s",
        "timeout": "1s"
      }
    ]
  }
}
