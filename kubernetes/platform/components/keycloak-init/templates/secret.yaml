---
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-config-cli-config-realms
  namespace: keycloak
{{- with .Files.Glob "resources/realm/*json" }}
data:
{{- range $path, $bytes := . }}
  {{ base $path }}: {{ tpl ($.Files.Get $path) $ | b64enc | quote }}
{{- end }}
{{- end }}
