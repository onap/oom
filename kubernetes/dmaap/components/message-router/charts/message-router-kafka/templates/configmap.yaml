kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-config-files
  namespace: {{ include "common.namespace" . }}
apiVersion: v1
data:
{{ tpl (.Files.Glob "resources/config/files/*").AsConfig . | indent 2 }}
---
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-config-scripts
  namespace: {{ include "common.namespace" . }}
apiVersion: v1
data:
{{ tpl (.Files.Glob "resources/config/scripts/*").AsConfig . | indent 2 }}