---
kind: Kustomization
apiVersion: kustomize.config.k8s.io/v1beta1

secretGenerator:
  - name: onap-docker-registry-key-kiali
    files:
      - secrets/.dockerconfigjson
    type: kubernetes.io/dockerconfigjson

resources:
  - kiali-instance.yaml
