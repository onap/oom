apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: servicea-cert
  namespace: istio-system
spec:
  secretName: servicea-cert
  commonName: servicea.gw.mesh.onap.eu
  dnsNames:
  - servicea.mesh.onap.eu
  issuerRef:
    name: selfsigned
    kind: Issuer
