{{- if and .Values.global.cmpv2_enabled .Values.global.cmpv2_testing }}
apiVersion: v1
kind: Secret
metadata:
  name: "{{ include "common.fullname" . }}-secret"
type: Opaque
data:
  ejbca-config.sh:
  {{ (.Files.Glob "resources/ejbca-config.sh").AsSecrets }}
{{ end -}}