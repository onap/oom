#################################################################
# Enable/disable and configure helm charts (ie. applications)
# to customize the central ONAP deployment.
#################################################################
aaf:
  enabled: true

dmaap:
  enabled: true
message-router:
  enabled: false
dmaap-bc:
  enabled: true
dmaap-dr-prov:
  enabled: true
dmaap-dr-node:
  enabled: false