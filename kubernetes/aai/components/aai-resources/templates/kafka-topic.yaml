{{- /* AAI EVENT TOPIC */ -}}
{{- if .Values.aai.notifications.enabled }}
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: {{ .Values.global.config.topics.aaiEvent | lower }}-kafka-topic
  labels:
    {{- include "common.labels" . | nindent 4 }}
    strimzi.io/cluster: {{ include "common.namespace" . }}-strimzi
    helm.sh/resource-policy: keep
spec:
  topicName: {{ .Values.global.config.topics.aaiEvent }}
---
{{- end }}

{{- /* DELTA EVENT TOPIC */ -}}
{{- if .Values.delta.events.enabled }}
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaTopic
metadata:
  name: {{ .Values.global.config.topics.aaiDeltaEvent | lower }}-kafka-topic
  labels:
    {{- include "common.labels" . | nindent 4 }}
    strimzi.io/cluster: {{ include "common.namespace" . }}-strimzi
    helm.sh/resource-policy: keep
spec:
  topicName: {{ .Values.global.config.topics.aaiDeltaEvent }}
{{- end }}
