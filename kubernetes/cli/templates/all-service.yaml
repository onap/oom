apiVersion: v1
kind: Service
metadata:
  name: cli
  namespace: "{{ .Values.nsPrefix }}-cli"
  labels:
    app: cli
spec:
  ports:
  - name: 80-port
    port: 80
    nodePort: 30260
  type: NodePort
  selector:
    app: cli
