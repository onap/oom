#{{ if not .Values.disableVfcVfcCatalog }}
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: vfc-catalog
  namespace: "{{ .Values.nsPrefix }}"
spec:
  replicas: {{ .Values.catalog.replicas }}
  selector:
    matchLabels:
      app: vfc-catalog
  template:
    metadata:
      labels:
        app: vfc-catalog
      name: vfc-catalog
    spec:
      hostname: vfc-catalog
      containers:
      - args:
        image:  {{.Values.catalog.image}}
        name: "vfc-catalog"
        env:
        - name: MSB_ADDR
          value: {{ tpl .Values.msbaddr . }}
        volumeMounts:
        - mountPath: /var/log/onap
          name: vfc-catalog-log
        - mountPath: /opt/vfc/catalog/config/log.yml
          name: vfc-catalog-logconfig
          subPath: log.yml
        ports:
        - containerPort: {{ .Values.catalog.port }}
        readinessProbe:
          tcpSocket:
            port: {{ .Values.catalog.port }}
          initialDelaySeconds: 5
          periodSeconds: 10
        imagePullPolicy: "{{ .Values.pullPolicy }}"
     - image: {{ .Values.image.filebeat }}
       imagePullPolicy: {{ .Values.pullPolicy }}
       name: filebeat-onap
       volumeMounts:
       - mountPath: /usr/share/filebeat/filebeat.yml
         name: filebeat-conf
         subPath: filebeat.yml
       - mountPath: /var/log/onap
         name: vfc-catalog-log
       - mountPath: /usr/share/filebeat/data
         name: vfc-catalog-data-filebeat
     volumes:
     - name: vfc-catalog-log
       emptyDir: {}
     - name: vfc-catalog-data-filebeat
       emptyDir: {}
     - name: filebeat-conf
       configMap:
         name: vfc-filebeat-configmap
     - name: vfc-catalog-logconfig
       configMap:
         name: vfc-catalog-log-configmap
      imagePullSecrets:
      - name: "{{ .Values.nsPrefix }}-docker-registry-key"
     restartPolicy: Always
#{{ end }}
