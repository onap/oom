apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: keycloak-cert
  namespace: istio-system
spec:
  secretName: keycloak-cert
  commonName: keycloak.gw.mesh.onap.eu
  dnsNames:
  - keycloak.gw.mesh.onap.eu
  issuerRef:
    name: selfsigned
    kind: Issuer
