---
apiVersion: v1
kind: ConfigMap
metadata: 
  name: api-handler-infra-app-config
  namespace: {{ include "common.namespace" . }}
data:
  {{- $yamlpath := printf "resources/config/overrides/api-handler-infra/override.yaml" -}}
  {{- $root := . }}
  {{- range $path, $bytes := .Files.Glob $yamlpath }}
  override.yaml: {{ $root.Files.Get $path | quote }}
  {{- end }}
