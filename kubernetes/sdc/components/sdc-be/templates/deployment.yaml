{{/*
# Copyright © 2017 Amdocs, AT&T, Bell Canada
# Modifications Copyright © 2018 ZTE
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#       http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
*/}}

apiVersion: apps/v1
kind: Deployment
metadata: {{- include "common.resourceMetadata" . | nindent 2 }}
spec:
  selector: {{- include "common.selectors" . | nindent 4 }}
  replicas: {{ .Values.replicaCount }}
  template:
    metadata: {{- include "common.templateMetadata" . | nindent 6 }}
    spec:
      initContainers: {{ include "common.certInitializer.initContainer" . | nindent 6 }}
      - name: {{ include "common.name" . }}-readiness
        command:
        - /app/ready.py
        args:
        - --container-name
        - "sdc-onboarding-be"
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        image: {{ include "repositoryGenerator.image.readiness" . }}
        imagePullPolicy: {{ .Values.global.pullPolicy | default .Values.pullPolicy }}
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 3m
            memory: 20Mi
      - name: {{ include "common.name" . }}-job-completion
        image: {{ include "repositoryGenerator.image.readiness" . }}
        imagePullPolicy: "{{ .Values.global.pullPolicy | default .Values.pullPolicy }}"
        command:
        - /app/ready.py
        args:
        - --job-name
        - {{ include "common.release" . }}-sdc-onboarding-be-cassandra-init
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 3m
            memory: 20Mi
      {{- if .Values.global.aafEnabled }}
      - name: {{ include "common.name" . }}-update-config
        image: {{ include "repositoryGenerator.image.envsubst" . }}
        imagePullPolicy: {{ .Values.global.pullPolicy | default .Values.pullPolicy }}
        command:
        - sh
        args:
        - "-c"
        - |
          export $(grep '^c' {{ .Values.certInitializer.credsPath }}/mycreds.prop | xargs -0)
          export KEYSTORE_PASS=$cadi_keystore_password_p12
          export KEYMANAGER_PASS=$cadi_keystore_password_p12
          export TRUSTSTORE_PASS=$cadi_truststore_password
          cp {{ .Values.certInitializer.credsPath }}/{{ .Values.certInitializer.keystoreFile }} /config-output
          cp {{ .Values.certInitializer.credsPath }}/{{ .Values.certInitializer.truststoreFile }} /config-output
          cd /config-input && \
          for PFILE in `find . -not -type d | grep -v -F ..`
          do
            envsubst <${PFILE} >/config-output/${PFILE}
            chmod 0644 /config-output/${PFILE}
          done
        resources:
          limits:
            cpu: 100m
            memory: 100Mi
          requests:
            cpu: 3m
            memory: 20Mi
        volumeMounts: {{ include "common.certInitializer.volumeMount" . | nindent 8 }}
        - name: {{ include "common.fullname" . }}-environments
          mountPath: /config-input/
        - name: sdc-environments-output
          mountPath: /config-output/
      {{- end }}
      - name: {{ include "common.name" . }}-update-configuration
        image: "{{ .Values.global.envsubstImage }}"
        imagePullPolicy: {{ .Values.global.pullPolicy | default .Values.pullPolicy }}
        command:
        - sh
        args:
        - -c
        - "cd /configuration-input && for PFILE in `find . -not -type d | grep -v -F ..`; do envsubst <${PFILE} >/configuration-output/${PFILE}; chmod 0644 /configuration-output/${PFILE}; done"
        env:
        - name: AAF_USERNAME
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: aaf_username}
        - name: AAF_PASSWORD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: aaf_password}
        - name: CS_USERNAME
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: sdc_user}
        - name: CS_PASSWORD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: sdc_password}
        - name: CS_TRUSTSTORE_PASSWORD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: cs_truststore_password}
        - name: DPC_USERNAME
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: dpc_username}
        - name: DPC_PASSWORD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: dpc_password}
        - name: SOD_USERNAME
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: sod_username}
        - name: SOD_PASSWORD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: sod_password}
        - name: CADI_TRUSTSTORE_PASSWORD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: cadi_truststore_password}
        - name: DMAAP_TRUSTSTORE_PWD
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: dmaap_truststore_pwd}
        - name: SDC_ACCESS_RESTRICTION_SECURITY_KEY
          valueFrom:
            secretKeyRef: {name: {{ include "common.release" . }}-sdc-secrets, key: sdc_access_restriction_security_key}
        volumeMounts:
        - name: {{ include "common.fullname" . }}-configuration
          mountPath: /configuration-input/
        - name: sdc-configuration-output
          mountPath: /configuration-output/
      - name: {{ include "common.fullname" . }}-change-folder-permissions
        image: "{{ .Values.global.busyboxRepository }}/{{ .Values.global.busyboxImage }}"
        imagePullPolicy: {{ .Values.global.pullPolicy | default .Values.pullPolicy | quote}}
        command: ["/bin/sh", "-c", "chown -Rf 352070:35953 /var/lib/jetty/config/catalog-be"]
        volumeMounts:
        - name: sdc-configuration-output
          mountPath: /var/lib/jetty/config/catalog-be/
      containers:
        - name: {{ include "common.name" . }}
          image: {{ include "repositoryGenerator.repository" . }}/{{ .Values.image }}
          imagePullPolicy: {{ .Values.global.pullPolicy | default .Values.pullPolicy }}
          {{- if .Values.global.aafEnabled }}
          command:
          - sh
          args:
          - "-c"
          - |
            sed -i '/trustStorePassword/d' ${JETTY_BASE}/startup.sh
            ${JETTY_BASE}/startup.sh
          {{- end }}
          ports: {{ include "common.containerPorts" . | nindent 10  }}
          {{ if eq .Values.liveness.enabled true }}
          livenessProbe:
            httpGet:
              path: /sdc2/rest/healthCheck
              port: {{ .Values.liveness.port }}
              scheme: {{ if (include "common.needTLS" .) }}HTTPS{{ else }}HTTP{{ end }}
            initialDelaySeconds: {{ .Values.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.liveness.timeoutSeconds }}
          {{ end }}
          readinessProbe:
            exec:
              command:
              - "/var/lib/jetty/ready-probe.sh"
            initialDelaySeconds: {{ .Values.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.readiness.timeoutSeconds }}
          resources: {{ include "common.resources" . | nindent 12 }}
          env:
          - name: ENVNAME
            value: {{ .Values.env.name }}
          - name: JAVA_OPTIONS
            value: {{ .Values.config.javaOptions }}
          - name: cassandra_ssl_enabled
            value: {{ .Values.config.cassandraSslEnabled | quote }}
          - name: HOST_IP
            valueFrom:
              fieldRef:
                fieldPath: status.podIP
          volumeMounts:
          - name: sdc-environments-output
            mountPath: /var/lib/jetty/chef-solo/environments/
          - name: sdc-environments-output
            mountPath: /var/lib/jetty/chef-solo/cookbooks/sdc-catalog-be/files/default/org.onap.sdc.p12
            subPath: org.onap.sdc.p12
          - name: sdc-environments-output
            mountPath: /var/lib/jetty/chef-solo/cookbooks/sdc-catalog-be/files/default/org.onap.sdc.trust.jks
            subPath: org.onap.sdc.trust.jks
          - name: {{ include "common.fullname" . }}-localtime
            mountPath: /etc/localtime
            readOnly: true
          - name: {{ include "common.fullname" . }}-logs
            mountPath: /var/log/onap
          - name: sdc-configuration-output
            mountPath: /var/lib/jetty/config/catalog-be/
          - name: {{ include "common.fullname" . }}-logback
            mountPath: /var/lib/jetty/config/catalog-be/logback.xml
            subPath: logback.xml
        # side car containers
        - name: {{ include "common.name" . }}-filebeat-onap
          image: {{ include "repositoryGenerator.image.logging" . }}
          imagePullPolicy: {{ .Values.global.pullPolicy | default .Values.pullPolicy }}
          volumeMounts:
          - name: {{ include "common.fullname" . }}-filebeat-conf
            mountPath: /usr/share/filebeat/filebeat.yml
            subPath: filebeat.yml
          - name: {{ include "common.fullname" . }}-logs
            mountPath: /var/log/onap
          - name: {{ include "common.fullname" . }}-data-filebeat
            mountPath: /usr/share/filebeat/data
          resources:
            limits:
              cpu: 100m
              memory: 100Mi
            requests:
              cpu: 3m
              memory: 20Mi
      volumes: {{ include "common.certInitializer.volumes" . | nindent 6 }}
      - name: {{ include "common.fullname" . }}-localtime
        hostPath:
          path: /etc/localtime
      - name: {{ include "common.fullname" . }}-filebeat-conf
        configMap:
          name: {{ include "common.release" . }}-sdc-filebeat-configmap
      - name: {{ include "common.fullname" . }}-data-filebeat
        emptyDir: {}
      - name: {{ include "common.fullname" . }}-logback
        configMap:
          name : {{ include "common.fullname" . }}-logging-configmap
          defaultMode: 0644
      - name: {{ include "common.fullname" . }}-environments
        configMap:
          name: {{ include "common.release" . }}-sdc-environments-configmap
          defaultMode: 0644
      - name: sdc-environments-output
        emptyDir:
          medium: "Memory"
      - name: {{ include "common.fullname" . }}-logs
        emptyDir: {}
      - name: {{ include "common.fullname" . }}-configuration
        configMap:
          name: {{ include "common.fullname" . }}-configuration-configmap
          defaultMode: 0644
      - name: sdc-configuration-output
        emptyDir:
          medium: "Memory"
    imagePullSecrets:
      - name: "{{ include "common.namespace" . }}-docker-registry-key"
