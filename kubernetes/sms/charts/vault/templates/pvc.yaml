kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: aaf-sms-data
  namespace: "{{ .Values.nsPrefix }}"
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  selector:
    matchLabels:
      name: "{{ .Values.nsPrefix }}-aaf-sms"
