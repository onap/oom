dependencies:
  - name: mariadb-galera
    version: ~8.x-0
    # repository: 'file://./components/mariadb-galera'
    condition: mariadb.enabled
  - name: etcd
    version: ~8.x-0
    # repository: 'file://./components/etcd'
    condition: etcd.enabled
  - name: mongo
    version: ~8.x-0
    # repository: 'file://./components/mongo'
    condition: mongo.enabled
  - name: cassandra
    version: ~8.x-0
    # repository: 'file://./components/cassandra'
    condition: cassandra.enabled
  - name: postgres
    version: ~8.x-0
    # repository: 'file://./components/postgres'
    condition: postgres.enabled
