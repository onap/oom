listenerservice:
  config:
    asdcAddress: sdc-be.{{ include "common.namespace" . }}:8443 #SDC-BE
    messageBusAddress: message-router.{{ include "common.namespace" . }} #Message-Router
    user: vid #SDC-username
    password: Kp8bJ4SXszM0WXlhak3eHlcse2gAw84vaoGGmJvUy2U #SDC-password
    pollingInterval: 15
    pollingTimeout: 60
    relevantArtifactTypes: TOSCA_CSAR
    consumerGroup: cds-id-local
    environmentName: AUTO
    consumerId: cds-id-local
    keyStorePassword:
    keyStorePath:
    activateServerTLSAuth : false
    isUseHttpsWithDmaap: false
    archivePath: /opt/app/onap/cds-sdc-listener/
    # Not sure about the correct path/name blueprintprocessor or may be something like this
    # blueprints-processor-grpc.{{ include "common.namespace" . }}
    grpcAddress: blueprints-processor-grpc # Blueprint processor
    grpcPort: 9111 # GRPC-Port
    authHeader: Basic Y2NzZGthcHBzOmNjc2RrYXBwcw==