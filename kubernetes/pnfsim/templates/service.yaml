apiVersion: v1
kind: Service
metadata:
  name: {{ include "pnfsim.fullname" . }}
  labels:
    {{- include "pnfsim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pnfsim.selectorLabels" . | nindent 4 }}
