#{{ if not .Values.disableSdncUeb }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: sdnc-ueb-listener
  namespace: {{ .Values.nsPrefix }}-sdnc
data:
{{ tpl (.Files.Glob "resources/ueb/ueb-listener.properties").AsConfig . | indent 2 }}
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: ueb-listener
  namespace: "{{ .Values.nsPrefix }}-sdnc"
spec:
  selector:
    matchLabels:
      app: ueb-listener
  template:
    metadata:
      labels:
        app: ueb-listener
      name: ueb-listener
    spec:
      initContainers:
      - command:
        - /root/ready.py
        args:
        - --container-name
        - sdnc-db-container
        - --container-name
        - sdnc-controller-container
        env:
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
        image: {{ .Values.image.readiness }}
        imagePullPolicy: {{ .Values.pullPolicy }}
        name: sdnc-ueb-readiness
      containers:
      - command:
        - /opt/onap/sdnc/ueb-listener/bin/start-ueb-listener.sh
        env:
        - name: PROPERTY_DIR
          value: /opt/onap/sdnc/data/properties
        - name: SDNC_CONFIG_DIR
          value: /opt/onap/sdnc/data/properties
        image: {{ .Values.image.ueblistener }}
        imagePullPolicy: {{ .Values.pullPolicy }}
        name: ueb-listener-container
        volumeMounts:
        - mountPath: /etc/localtime
          name: localtime
          readOnly: true
        - mountPath: /opt/onap/sdnc/data/properties/ueb-listener.properties
          name: ueb-config
      volumes:
        - name: localtime
          hostPath:
            path: /etc/localtime
        - name: ueb-config
          configMap:
            name: sdnc-ueb-listener
      imagePullSecrets:
      - name: "{{ .Values.nsPrefix }}-docker-registry-key"
#{{ end }}