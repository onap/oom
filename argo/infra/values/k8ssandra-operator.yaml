global:
  clusterScoped: true

image:
  registry: dockerhub.devops.telekom.de
  registryOverride: dockerhub.devops.telekom.de
  #registry: docker.io
  #registryOverride: docker.io

cass-operator:
  image:
    registry: dockerhub.devops.telekom.de
    registryOverride: dockerhub.devops.telekom.de
    #registry: docker.io
    #registryOverride: docker.io
  admissionWebhooks:
    enabled: true

cleaner:
  image:
    registry: dockerhub.devops.telekom.de
    #registry: docker.io

client:
  image:
    registry: dockerhub.devops.telekom.de
    #registry: docker.io

# -- Allows managing CRD upgrades externally and fully disable the CRD upgrader job hook
disableCrdUpgraderJob: true
