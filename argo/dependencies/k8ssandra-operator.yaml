apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: k8ssandra-operator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
  - repoURL: 'https://gitlab.devops.telekom.de/tnap/development/playground/onap-argo.git'
    targetRevision: onap-00
    ref: defaultValues
  - repoURL: https://helm.k8ssandra.io/stable
    chart: k8ssandra-operator
    targetRevision: 1.23.1
    helm:
      helm:
      ignoreMissingValueFiles: true
      valueFiles:
      - $defaultValues/argo/dependencies/values/k8ssandra-operator.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: k8ssandra-operator
  syncPolicy:
    managedNamespaceMetadata:
      labels:
        istio-injection: enabled
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    automated:
      prune: true
      selfHeal: true
