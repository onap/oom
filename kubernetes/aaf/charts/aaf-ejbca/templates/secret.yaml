{{- if and .Values.global.cmpv2_enabled.enabled .Values.global.cmpv2_testing.enabled }}
apiVersion: v1
kind: Secret
metadata:
  namespace: default
  name: "{{ include "common.fullname" . }}-database-credentials"
type: Opaque
data:
  # "ejbca"
  username: ZWpiY2E=
  # "ejbca"
  password: ZWpiY2E=
  # "foo123"
  password-root: Zm9vMTIz
---
{{ end -}}