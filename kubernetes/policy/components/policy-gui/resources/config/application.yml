server:
  port: 2443
  ssl:
    enabled: false

clamp:
  url:
  disable-ssl-validation: true
  disable-ssl-hostname-check: true

apex-editor:
  upload-url:
  upload-userid:

management:
  endpoints:
    web:
      exposure:
        include: health, metrics, prometheus
