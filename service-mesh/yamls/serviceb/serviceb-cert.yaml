apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: serviceb-cert
  namespace: istio-system
spec:
  secretName: serviceb-cert
  commonName: serviceb.gw.mesh.onap.eu
  dnsNames:
  - serviceb.mesh.onap.eu
  issuerRef:
    name: selfsigned
    kind: Issuer
