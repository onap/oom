apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: wordpress-cert
  namespace: istio-system
spec:
  secretName: wordpress-cert
  commonName: wordpress.gw.mesh.onap.eu
  dnsNames:
  - wordpress.gw.mesh.onap.eu
  issuerRef:
    name: selfsigned
    kind: Issuer
