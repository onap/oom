#{{ if not .Values.disableMulticloudMulticloudVio }}
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: multicloud-vio
  namespace: "{{ .Values.nsPrefix }}-multicloud"
spec:
  selector:
    matchLabels:
      app: multicloud-vio
  template:
    metadata:
      labels:
        app: multicloud-vio
      name: multicloud-vio
    spec:
     containers:
     - env:
       - name: MSB_ADDR
         value: "{{ .Values.msbgateway }}"
       - name: MSB_PORT
         value: "{{ .Values.msbPort }}"
       - name: AAI_ADDR
         value: aai-service.{{ .Values.nsPrefix }}-aai
       - name: AAI_PORT
         value: "8443"
       - name: AAI_SCHEMA_VERSION
         value: "v11"
       - name: AAI_USERNAME
         value: "AAI"
       - name: AAI_PASSWORD
         value: "AAI"
       name: multicloud-vio
       volumeMounts:
       - mountPath: /opt/vio/logs
         name: vio-log
       - mountPath: /opt/vio/vio/log.conf
         name: vio-logconf
       image: {{ .Values.image.vio }}
       imagePullPolicy: {{ .Values.pullPolicy }}
       ports:
       - containerPort: 9004
       livenessProbe:
         httpGet:
           path: /api/multicloud-vio/v0/swagger.json
           port: 9004
           scheme: HTTP
         initialDelaySeconds: 30
         periodSeconds: 10
         timeoutSeconds: 10
         successThreshold: 1
         failureThreshold: 5
     - image: {{ .Values.image.filebeat }}
       imagePullPolicy: {{ .Values.pullPolicy }}
       name: filebeat-onap
       volumeMounts:
       - mountPath: /usr/share/filebeat/filebeat.yml
         name: filebeat-conf
       - mountPath: /var/log/onap/multicloud/vio
         name: vio-log
     volumes:
     - name: vio-log
       emptyDir: {}
     - name: filebeat-conf
       hostPath:
        path: /dockerdata-nfs/{{ .Values.nsPrefix }}/log/filebeat/logback/filebeat.yml
     - name: vio-logconf
       hostPath:
        path: /dockerdata-nfs/{{ .Values.nsPrefix }}/log/filebeat/multicloud/vio_log.conf
     imagePullSecrets:
     - name: "{{ .Values.nsPrefix }}-docker-registry-key"
     restartPolicy: Always
#{{ end }}