{{- if and .Values.global.cmpv2_enabled.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "common.servicename" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ .Values.appLabel }}
  ports:
    - protocol: TCP
      port: {{ .Values.containerPort }}
{{ end -}}