---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-ingress
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    name: infra-ingress
spec:
  project: argo-management
  sources:
  - repoURL: 'https://git.onap.org/oom'
    targetRevision: master
    ref: defaultValues
  - repoURL: https://git.onap.org/oom
    targetRevision: master
    path: ./argo/dependencies/ingress-routes/helm
    helm:
      ignoreMissingValueFiles: true
      valueFiles:
      - $defaultValues/argo/dependencies/values/infra-ingress.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - Validate=true
    - CreateNamespace=true
    #- PrunePropagationPolicy=foreground
    #- PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
