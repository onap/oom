apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: auth-cert
  namespace: istio-system
spec:
  secretName: auth-cert
  commonName: auth.gw.mesh.onap.eu
  dnsNames:
  - auth.gw.mesh.onap.eu
  issuerRef:
    name: selfsigned
    kind: Issuer
