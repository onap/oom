kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: aai-resources-config
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  selector:
    matchLabels:
      name: aai-resources-config
