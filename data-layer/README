This is an initial incomplete version of ONAP data layer missing elasticsearch
To deploy: helm install --set global.masterPassword=pleaseMakeMeMoreSecure data-layer-onap .

input_overrides.yaml is used to show how get_secret.sh works
In a real environment, output_overrides.yaml should not be created on disk to avoid leaving passwords on disk. 
Instead, the script should run:
envsubst < input_overrides.yaml | kubectl apply -f -
or pass the file to "helm install" in a similar way